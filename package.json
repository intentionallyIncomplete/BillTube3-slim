{
  "name": "billtube3-slim",
  "version": "3.4.0",
  "description": "Modular UI enhancement framework for CyTube",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint-staged": "lint-staged",
    "lint": "eslint . --ext .js,.css,.md,.json",
    "lint:fix": "npm run lint:js -- --fix && npm run lint:css -- --fix",
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "purge-cdn": "curl -s -X GET \"https://purge.jsdelivr.net/gh/intentionallyIncomplete/BillTube3-slim@dev/channel_config_settings.js\" && echo \"✓ CDN cache purged\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intentionallyIncomplete/BillTube3-slim.git"
  },
  "author": "Quigly",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.0",
    "eslint": "^8.57.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  },
  "lint-staged": {
    "*": "npm run lint"
  }
}

